<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from
        being saved:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :user_name %>
  </div>

  <div class="field">
    <%= form.label :card_id %>
    <%= form.collection_select :card_id, Card.available_for_user(user), :id, :uid, prompt: true %>
    <!--< %= form.fields_for :card, include_id: false do |f| %>-->
    <!--< %= f.hidden_field :user_id, value:user.id %>-->
    <!--< % end %>-->
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
